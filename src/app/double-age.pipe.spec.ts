import { DoubleAgePipe } from './double-age.pipe';

describe('DoubleAgePipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleAgePipe();
    expect(pipe).toBeTruthy();
  });
});
